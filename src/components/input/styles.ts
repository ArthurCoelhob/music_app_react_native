import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentInput: {
    padding: 5,
  },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 12,
    backgroundColor: '#fff'
  }
})